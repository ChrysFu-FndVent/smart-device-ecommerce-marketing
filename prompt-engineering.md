# Prompt 工程 · 跨品类商品营销的 4 种可验证模式

> 资产类型：参数化 Prompt 模式库
> 用途：覆盖采集编码、数据解读、受控文案和视觉生成。每种模式都先声明品类配置与客观验收落点，再让 AI 生成候选。

## 使用前替换变量

| 变量 | 内容 | 不允许的默认行为 |
|---|---|---|
| `{CATEGORY}` | 商品品类与子品类范围 | 模型自行扩大到相邻品类 |
| `{DIMENSION_SCHEMA}` | 当前品类的属性维度与标注规则 | 直接复用智能手环 14 维 |
| `{SPEC_SOURCE}` | 正式规格、说明书、检测或授权资料 | 从评论或图片猜测参数 |
| `{RISK_RULES}` | 禁用词、资质和必要声明 | 模型自行判断合规 |
| `{CHANNEL_BRIEF}` | 受众、渠道、尺寸、CTA 和品牌要求 | 使用通用“爆款”模板 |

## 模式一 · 约束式采集与分析代码生成

```text
为以下商品文本任务编写可测试的 Python 数据流水线。

【品类配置】
- CATEGORY: {CATEGORY}
- 商品标识与同义词: {CATEGORY_TERMS}
- 分析维度: {DIMENSION_SCHEMA}
- 品类停用词: {PRODUCT_STOPWORDS}

【数据来源】
- 输入 URL/文件及来源类型: {SOURCE_LIST}
- 可访问与授权边界: {ACCESS_RULES}

【硬性要求】
1. 输出保留 product_id/source_type/source_url/captured_at；
2. 解析器失败时记录日志，不绕过登录、验证码或访问控制；
3. 原始层、清洗层和分析层分文件保存，支持幂等重跑；
4. 为解析、去重、维度标注和空值规则各写至少一个测试；
5. 先输出模块结构、字段 Schema 和测试清单，确认后再实现。
```

**验收落点**：代码实际运行；字段契约完整；相同输入可重复得到一致结果；失败来源有记录；维度标注能与人工试标集对照。

## 模式二 · 强制引用的数据解读

```text
基于以下冻结版本的统计结果，生成营销洞察候选。

【品类与样本边界】{CATEGORY_AND_SAMPLE_SCOPE}
【统计结果】{STATISTICS_OUTPUT}

【硬性要求】
1. 每条以「数据观察：」开头，并以「数据依据：指标、数值、分母」结尾；
2. 超出统计结果的解释必须另起一行，以「假设（待验证）：」开头；
3. 每条提供一个反面解释或样本偏差解释；
4. 不出现输入中不存在的数字，不把提及率写成购买重要度；
5. 品牌/单品样本低于 {MIN_SAMPLE} 时只标记风险，不排序优劣；
6. 输出原文回查所需的 record_id 或统计表位置。
```

**案例验证**：智能手环案例中，“5 星占 70.9%”同时保留了“测评/晒物来源可能天然正偏”的反面解释，避免把偏正样本外推为市场口碑。

## 模式三 · 受控电商文案

```text
为 {CATEGORY} 生成 {TARGET_CHANNEL} 的卖点文案候选。

【目标用户与场景】{AUDIENCE_AND_SCENARIO}
【可用数据观察】{APPROVED_INSIGHTS}
【正式规格源】{SPEC_SOURCE}
【禁用词与禁止主张】{FORBIDDEN_CLAIMS}
【允许表达与必备声明】{APPROVED_LANGUAGE_AND_DISCLOSURES}
【格式要求】{CHANNEL_BRIEF}

【输出要求】
1. 每条文案标注使用的数据观察和规格条目；
2. 数据只决定信息优先级，具体参数必须来自正式规格源；
3. 缺失规格写入 [CHECK] 清单，不得补全；
4. 列出所有功能、性能、功效、材质和认证主张；
5. 同时输出一个更克制的备选版本。
```

### 案例实例化 · 智能手环

- `{APPROVED_INSIGHTS}`：健康监测 38.8%、安全防护 18.7%、外观佩戴 17.9%、远程监护 14.9%，只代表当前样本；
- `{FORBIDDEN_CLAIMS}`：医疗级、诊断、替代体检、100% 准确、治疗、无创血糖检测；
- `{APPROVED_LANGUAGE}`：趋势监测、异常预警、健康参考、日常管理助手；
- `{SPEC_SOURCE}`：实际产品规格、检测依据和资质文件，不使用评论替代。

迁移到美妆、食品、儿童用品、户外装备或家电时，必须由对应品类负责人重建禁用词、资质与声明要求。

## 模式四 · 证据先行的视觉生成

```text
生成 {CATEGORY} 的 {ASSET_TYPE} 概念稿。

【受众与使用场景】{AUDIENCE_AND_SCENARIO}
【视觉优先级】{APPROVED_VISUAL_HIERARCHY}
【已确认规格】{VERIFIED_SPECS}
【待核对项】{UNVERIFIED_ITEMS}
【品牌与渠道规范】{BRAND_AND_CHANNEL_RULES}
【禁止元素】{PROHIBITED_VISUALS}

【输出要求】
1. 待核对参数不进入成品文字，只保留占位；
2. 不生成不存在的认证、检测、奖项、包装或配件；
3. 为每个功能性视觉元素输出溯源编号；
4. 预留必要声明、价格条件和 CTA 区域；
5. 输出图片后列出所有可见文字，便于人工校对。
```

### 案例实例化 · 智能手环海报

案例使用银发用户与家属远程查看的双场景，优先展示健康趋势、安全提醒和适老交互。第一版出现医生与听诊器，超出健康管理定位；加入禁止医疗诊断暗示后移除。图中的续航、定位和防水文字仍属于概念占位，不能替代规格核验。

## 四种模式的共同验收

| 模式 | 客观验收锚点 |
|---|---|
| 代码生成 | 能运行、字段和测试齐全、失败有日志 |
| 数据解读 | 每个数字回到冻结统计结果和分母 |
| 受控文案 | 每个商品主张回到数据、规格或合规依据 |
| 视觉生成 | 每个功能元素有溯源编号，所有可见文字可核对 |

可靠性的来源不是更长的 Prompt，而是可版本化的品类输入、明确的失败行为和人工能够执行的验收锚点。这与[网页采集 Agent 的结构化输出校验](https://github.com/ChrysFu-FndVent/web-data-agent/blob/main/prompt-engineering.md)及[机构内容工作流的待核对清单](https://github.com/ChrysFu-FndVent/institutional-content-ops/blob/main/prompt-engineering.md)采用相同原则。
